# Sample environment for ha.sh + pws-report.sh
# Copy to .env and fill in the values.

# Enable debug logging (0/1)
DEBUG=0

# Home Assistant connection
# Example: http://homeassistant.local:8123
HA_HOST=http://homeassistant.local:8123
# Long-lived access token from Home Assistant
HA_API_TOKEN=

# Home Assistant entity matching
# Match entity_ids containing this string
HA_ENTITY_MATCH=vevor
# Remove this prefix from matched entity_id names (optional)
HA_ENTITY_PREFIX=sensor.vevor_7in1_0_8993_
# Space-separated mapping of sender parameters to HA keys (param:ha_key)
# Supported params include:
# temperatureC temperatureF humidity windSpeedKph windSpeedMps windSpeedMph
# windGustKph windGustMps windGustMph windDirDeg rainTotalMm uvIndex
# outsideLuminanceLux dewpointC dewpointF pressureHpa pressurePa
HA_ENTITY_MAP="temperatureC:temperature humidity:humidity windSpeedKph:wind_speed windGustKph:wind_max_speed windDirDeg:wind_direction rainTotalMm:rain_total uvIndex:uv_index outsideLuminanceLux:outside_luminance"

# Destination enable flags
APRS_ENABLE=0
WINDY_ENABLE=0

# Observation interval in seconds (minimum 300)
REPORT_INTERVAL=300

# Optional: dry-run mode (1 = do not send, only log)
APRS_DRY_RUN=0
WINDY_DRY_RUN=0

# Windy integration (required when WINDY_ENABLE=1)
WINDY_STATION_ID=
WINDY_PASSWORD=
WINDY_URL=https://stations.windy.com/api/v2/observation/update

# APRS-IS settings used by aprs-weather-submit (required when APRS_ENABLE=1)
APRS_CALLSIGN=
APRS_LATITUDE=
APRS_LONGITUDE=
# Optional overrides (defaults shown)
APRS_SERVER=rotate.aprs2.net
APRS_PORT=14580
APRS_USERNAME=
APRS_PASSWORD=

# Optional: altitude in meters (converted to feet for APRS)
APRS_ALTITUDE_M=

# Optional: lux-to-W/m^2 efficacy (used when outsideLuminanceLux is present)
# Default is 110 if unset
LUX_EFFICACY=110

# Optional: cache file for pws-report.sh
PWS_CACHE_FILE=.pws-report.cache
